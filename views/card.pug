doctype html
html(lang="en")
    head
        title Flashcards
    body
        header
            h1 Flashcards
        section#content
            h2= prompt
            if hint
                p
                    i Hint: #{hint}
            else
                p
                    i (There is no hint)
        footer
            p An app to help you study